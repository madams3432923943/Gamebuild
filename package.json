{
  "name": "ball-knowledge",
  "description": "An NBA draft-battle game: draft a shared team-and-decade squad from memory, then play out the result.",
  "repository": "github:madams3432923943/gamebuild",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "esbuild js/main.js --bundle --format=iife --outfile=dist/bundle.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
