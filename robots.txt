# Draft Nova is a game, not a corpus. Everything a crawler can usefully index
# lives on one page; the rest is a signed-in app whose URLs are all the same URL.
User-agent: *
Allow: /
# Generated datasets and build tooling: thousands of player rows that say
# nothing about the game and cost a crawl budget to fetch.
Disallow: /data/
Disallow: /tools/
Disallow: /scripts/
Disallow: /db/
# The internal dashboard now lives under /tools/, which the line above already
# disallows. It is opened with `npm run admin` on localhost rather than being a
# page of this site at all - and authorization is enforced server-side either
# way (db/migrations/20260911_03_admin_dashboard.sql), so no crawler rule is
# what protects it.

Sitemap: https://draftnovagame.com/sitemap.xml
